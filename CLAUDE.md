# CLAUDE.md — 이 저장소 사용법

이 저장소는 **새 서비스를 개발하기 위한 참조 명세**입니다. 실행 코드는 없습니다.
33m2와 Airbnb를 역설계해 얻은 화면·플로우·데이터 모델·API·권한 규칙이 들어 있습니다.

## 새 서비스가 무엇인가

**중장기 임대(한달살기·단기임대) + 숙박 예약**을 결합한 국내 서비스.
33m2의 계약·정산·에스크로 구조에 Airbnb의 검색·신뢰·가격 유연성을 얹는 방향.
**웹 먼저 런칭 → 이후 앱 2차 런칭.**

> ⚠️ 새 서비스의 최종 방향성은 **아직 확정되지 않았습니다**.
> 확정 전에는 `_synthesis/comparison.md`의 "가져올 것 / 버릴 것"을 제안으로만 취급하고,
> 임의로 제품 결정을 내리지 마세요.

---

## 읽는 순서

| 목적 | 파일 |
|---|---|
| **전체 맥락부터** | `_synthesis/comparison.md` — 두 서비스 대조 + 강약점 + 권장 |
| **완성도·한계** | `_synthesis/GAP-ANALYSIS.md` — 무엇이 확실/추정/미확인인지 |
| 계약 도메인·상태머신·서브플로우 | `33m2/DOMAIN.md` ★ |
| 요금 계산 산식 | `33m2/guest/flows.md` §FLOW-G03, `_synthesis/comparison.md` §2 |
| **데이터 모델 (전체)** | `33m2/data-model.md` ★ (179 DTO/1,045필드) · `33m2/zod-schemas.md` (웹) |
| **API 엔드포인트 (전체)** | `33m2/api-endpoints.md` ★ (159개) · `33m2/api.md` (규약·의미) |
| 인증·계정·본인인증 | `33m2/auth-and-account.md` |
| enum·에러코드 | `33m2/enums.md` |
| 호스트 입력 모델 | `33m2/host/screens-and-model.md` §SCR-H03 |
| 권한 | `_synthesis/permissions.md` (PERM-01~08) |
| 화면 인벤토리 | `33m2/guest/screens.md`, `33m2/host/screens-and-model.md` |
| **모바일 앱 (2차 런칭)** | `_synthesis/mobile-apps.md` ★ |
| 디자인 시스템 | `33m2/design-system.md` · `airbnb/design-system.md` |
| UI 문구·i18n | `33m2/i18n-reference.md` (79 네임스페이스/1,876키) |
| Airbnb (참고) | `airbnb/guest/{listing-entity,pdp-sections,search-filters,checkout}.md` |

---

## ID 체계

교차 참조용 안정 ID. 새 문서를 쓸 때도 이 규칙을 따르세요.

| 접두사 | 의미 | 예 |
|---|---|---|
| `SCR-G##` / `SCR-H##` | 화면 (Guest / Host) | `SCR-G03` 매물 상세 |
| `SCR-A##` | Airbnb 화면 | `SCR-A03` PDP |
| `FLOW-G##` | 유저 플로우 | `FLOW-G02` 계약 상태 머신 |
| `ENT-###` | 엔티티 | `ENT-001` Room |
| `API-33-##` / `API-AB-##` | 관찰된 엔드포인트 | `API-33-03` 일정 조회 |
| `PERM-##` | 권한 규칙 | `PERM-02` 접근 매트릭스 |

---

## 신뢰도 표기 — 반드시 지킬 것

문서 전반에서 다음 마커를 씁니다. **코드를 생성할 때 `❓` 항목을 사실로 취급하지 마세요.**

| 마커 | 의미 | 취급 |
|---|---|---|
| `✅ 검증` | 데이터로 교차 확인 (예: 수수료율 7건 일치) | 그대로 사용 가능 |
| `👁 관찰` | UI에서 확인, 수치 교차검증은 없음 | 사용하되 주석에 근거 남길 것 |
| `❓ 추정` | 미검증 추론 | **구현 전 확인 필요**. 값을 하드코딩하지 말 것 |

---

## 확정된 핵심 수치 (✅ 검증됨)

이것들은 근거가 명확하므로 참조해도 됩니다.

```
33m2 임차인 계약 수수료 = (임대료 + 관리비 + 청소비) × 9.9%
33m2 임대인 계약 수수료 = 이용금액 × 3.3%          ← 정산 7건 전부 ×0.967 일치
33m2 보증금            = 330,000원 고정 (호스트 설정 불가)
33m2 계약 기간         = 1~12주 (초과 시 연장 신청)
33m2 정산 시점         = 입주일 D+0 ~ D+2
33m2 환불 프리셋       = WEAK / MIDDLE / STRONG
   15일 전: 무료/10%/20% · 8일 전: 20%/30%/50% · 1일 전: 40%/50%/70% · 당일 이후: 취소 불가
33m2 매물 사진         = 최소 6장 / 최대 20장
Airbnb 사진            = 최소 5장
33m2 앱 REST API   = 159 엔드포인트 (APK 추출, 33m2/api-endpoints.md)
33m2 데이터 모델    = 179 DTO / 1,045 필드 (APK @SerializedName, 33m2/data-model.md)
33m2 쓰기 API       = REST (웹 Server Action은 REST 래퍼) — 계약요청/승인/거절/취소/환불/퇴실확인/리뷰답글
33m2 앱 = 네이티브 Kotlin+OkHttp · Airbnb 앱 = Apollo GraphQL
Airbnb 건물 유형       = 31종 · 33m2 = 12종
```

---

## 절대 하지 말 것

1. **두 서비스의 코드·디자인 자산·이미지·문안을 복제하지 마세요.** 이 문서는 *동작과 구조*의 기록입니다. UI 텍스트를 그대로 가져다 쓰지 마세요.
2. **`❓ 추정` 값을 상수로 박지 마세요.** 특히 Airbnb 호스트 수수료율은 **미확인**입니다.
3. **관찰된 결함을 그대로 복제하지 마세요.** 명시적으로 표시해 두었습니다:
   - 33m2의 "선결제자 확정" 경쟁 승인 (임차인에게 리스크 전가)
   - 33m2의 `propertyType` 표기 불일치 (목록=한글, 상세=enum) → **enum으로 단일화**
   - 33m2의 매물 상세 새 탭 강제 열기

---

## 구현 시 반드시 반영할 제약

- **주소·연락처 공개 시점**: 계약 확정 전에는 지번/도로명까지, 확정 후 상세주소(층·호수)와 양측 전화번호 공개 (`PERM-02`)
- **비로그인 열람 허용**: 검색·상세·요금·리뷰는 로그인 없이 (SEO). 찜·요청·채팅만 게이트
- **금액은 서버가 계산**: 클라이언트가 할인·수수료를 재계산하지 않도록 계산 결과를 함께 내려줄 것
- **가용 일정 표현**: 점유일만 반환하고 미포함 = 가용 (`API-33-03`)
- **국내 필수**: 카카오톡 알림 채널, 방해금지 시간대, 거래성 알림은 설정 무관 발송
- **법적 지위**: 통신판매 중개자(거래 당사자 아님). 보증금 에스크로는 자금 보관이므로 **법무 검토 필요**

---

## 아직 비어 있는 것 (33m2 완성도 ~95%)

원리상 추출 불가하거나 조건이 안 맞는 것들 (`_synthesis/GAP-ANALYSIS.md` 참조):
- **HTTP 메서드** — 경로·모델은 확실, 메서드는 RESTful 규약 추론 (R8이 상수화)
- **Firebase RDB 채팅 경로 스키마** — 실 메시지 스트림 관찰 필요
- **퇴실 점검·보증금 차감 UI** — 해당 상태의 계약이 없어 미관찰 (단, API·플로우는 확보)
- **토스페이먼츠/MOK 콜백 상세** — 실결제·실인증 필요
- **Airbnb 호스트 수수료율** — 등록 숙소 0건 조건

새 서비스의 화면/플로우/API 명세는 **아직 작성하지 않았습니다** — 방향성 확정 후 `_synthesis/`에 추가할 예정입니다.
